# Terraform (Watchtower)

Starter layout for **GCP**. Replace placeholders before `apply`.

- `environments/staging/` — example root module; copy for `prod` when ready.
- `modules/` — add reusable modules (Cloud SQL, Cloud Run, Scheduler) incrementally.

See [docs/plans/gcp-terraform.md](../docs/plans/gcp-terraform.md) for the full checklist.
